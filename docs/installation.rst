============
Installation
============

At the command line either via pip::

    $ pip install yelp_avro

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv yelp_avro
    $ pip install yelp_avro
